
using UnityEngine;

public class checkcrate0 : MonoBehaviour
{
    public Crates cratePrefab;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
         
    }
}

